# Skills Placeholder

This folder documents the intended `skills/` layout for the Pattern 4 package.

The runtime loads skill files from `_shared/skills` so the actual skill source of truth stays centralized at the repository root.
