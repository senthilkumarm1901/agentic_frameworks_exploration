[project]
name = "ollama-adapter"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "strands-agents>=0.1",
    "openai>=1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ollama_adapter"]
